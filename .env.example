# Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env file with actual credentials to version control

# MySQL Database Configuration
# Use strong, unique passwords for production environments
MYSQL_ROOT_PASSWORD=your_secure_root_password_here
MYSQL_DATABASE=crud_flask
MYSQL_USER=your_database_user
MYSQL_PASSWORD=your_secure_user_password_here

# Flask Application Configuration
# Generate a strong secret key (e.g., python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=your_secret_key_here
FLASK_ENV=development

# Instructions:
# 1. Copy this file to .env: cp .env.example .env (or copy .env.example .env on Windows)
# 2. Replace all placeholder values with your actual credentials
# 3. Never commit the .env file to version control
# 4. For production, set FLASK_ENV=production and use strong passwords
